<!DOCYTPE html>
<html>
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%# <a class="navbar-brand" href="#">AceBook</a> %>
        <ul class="nav navbar-nav navbar-left">
          <%= link_to 'AgeBook', '/posts', class: 'navbar-brand'%>
          <% if logged_in? %>
            <%= link_to 'My Profile', user_page_path(current_user.id), class: 'navbar-brand'%>
            <div class='navbar-brand'>Logged in as <%= current_user.username %></div>
          <% end %>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if logged_in? %>
              <li><%= link_to "Log Out", logout_path, class: 'navbar-brand' %></li>
          <% end %>
        </ul>         
      </div>
    </div><!-- /.container-fluid -->
  </nav>
</html>
