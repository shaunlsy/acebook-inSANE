<div class='all-users'>
  <h1 align="center">Profiles</h1>
  <% @users.each do |user| %>
      <% if user.username != current_user.username %>
        <%= link_to user.username, user_page_path(User.find(user.id))%><br>
      <% end %>  
  <% end %>
</div>

  