
<% obj.reverse.each do |post| %>
  <br>
  <div class="box">
    <div class="box-header">    
      <h3><img src="https://images.pexels.com/photos/736230/pexels-photo-736230.jpeg?auto=compress&cs=tinysrgb&dpr=1&w=500" alt="" />
        <%= link_to User.find(post.user_id).username, user_page_path(User.find(post.user_id))%>
        <span>Updated <%= time_ago_in_words(post.updated_at) %> ago, <i class="fa fa-globe"></i></span>
        <span>Created at <%= post.created_at.strftime("%Y/%m/%d %H:%M:%S") %><i class="fa fa-globe"></i></span>
      </h3>
      <span><i class="fa fa-angle-down"></i></span>
      <div class="window"><span></span></div>
    </div>
    <div class="box-content">
      <div class="content">
        <div class="bottom">
          <p><%= post.message.gsub(/\n/, '<br/>').html_safe %></p>
          <span><span class="fa fa-search-plus"></span></span>
          <div class="next-bottom">
            <button class="button" id="delete-button"><%= link_to 'Delete', post_path(post), method: :delete %></button>
            <% if post.created_at > 10.minutes.ago %> 
              <button class="button" id="update-button"><%= link_to 'Update', edit_post_path(post) %></button>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    <div class="box-likes">
      <div class="row">
        <span><a href="#"><img src="" alt="" /></a></span>
        <span><a href="#"><img src="" alt="" /></a></span>
        <span><a href="#"><img src="" alt="" /></a></span>
        <span><a href="#">+92345675432229</a></span>
        <span>Like this</span>
      </div>
      <div class="row">
        <span>123456789765432 comments</span>
      </div>
    </div>
  </div>
<% end %>




